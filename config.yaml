server:
  port: ":8080"

database:
  driver: "sqlite" # 또는 postgres (코드에 따라 다름)
  # ❌ 기존: file: "todos.db"
  # ✅ 변경: 볼륨이 연결된 /data 폴더 밑에 저장
  file: "/data/todos.db"

log:
  level: "info"
  path: "./logs/server.log"
  max_size: 10
  max_backups: 5
  max_age: 30